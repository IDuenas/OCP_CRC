# Schema template for clusters/crc/services/openshift-config/cluster-role-binding.yaml
# The real file is gitignored and generated by setup.sh — do NOT commit the real file.
#
# To regenerate: run ./setup.sh
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: keycloak-cluster-admin-<KEYCLOAK_ADMIN_USER>
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: cluster-admin
subjects:
  - kind: User
    name: <KEYCLOAK_ADMIN_USER>
