# Schema template for clusters/crc/services/openshift-config/oidc-secret.yaml
# The real file is gitignored and generated by setup.sh — do NOT commit the real file.
#
# To regenerate: run ./setup.sh
apiVersion: v1
kind: Secret
metadata:
  name: keycloak-oidc-secret
  namespace: openshift-config
type: Opaque
stringData:
  clientSecret: <OIDC_CLIENT_SECRET>
