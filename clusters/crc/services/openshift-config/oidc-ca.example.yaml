# Schema template for clusters/crc/services/openshift-config/oidc-ca.yaml
# The real file is gitignored and generated by setup.sh — do NOT commit the real file.
#
# To regenerate: run ./setup.sh (requires local-ca.crt to be present)
apiVersion: v1
kind: ConfigMap
metadata:
  name: keycloak-oidc-ca
  namespace: openshift-config
data:
  ca.crt: |
    -----BEGIN CERTIFICATE-----
    <LOCAL_CA_CERTIFICATE_CONTENT>
    -----END CERTIFICATE-----
