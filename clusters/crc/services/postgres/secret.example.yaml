# Schema template for clusters/crc/services/postgres/secret.yaml
# The real file is gitignored and generated by setup.sh — do NOT commit the real file.
#
# To regenerate: run ./setup.sh
apiVersion: v1
kind: Secret
metadata:
  name: keycloak-db-secret
  namespace: postgres
  annotations:
    argocd.argoproj.io/sync-wave: "-1"
type: Opaque
stringData:
  POSTGRES_DB: keycloak
  POSTGRES_USER: keycloak
  POSTGRES_PASSWORD: <DB_PASSWORD>
