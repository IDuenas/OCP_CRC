# Schema template for clusters/crc/services/keycloak/admin-secret.yaml
# The real file is gitignored and generated by setup.sh — do NOT commit the real file.
#
# To regenerate: run ./setup.sh
apiVersion: v1
kind: Secret
metadata:
  name: custom-keycloak-admin-secret
  namespace: keycloak
type: Opaque
stringData:
  username: <KEYCLOAK_ADMIN_USER>
  password: <KEYCLOAK_ADMIN_PASSWORD>
