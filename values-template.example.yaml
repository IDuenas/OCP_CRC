# Schema template for values-template.yaml
# The real file is gitignored and auto-generated by setup.sh — do NOT commit the real file.
#
# To regenerate: run ./setup.sh
cluster:
  domain: apps-crc.testing
keycloak:
  admin_user: <KEYCLOAK_ADMIN_USER>
  hostname: keycloak-keycloak.apps-crc.testing
tls:
  provider: local-ca  # Options: local-ca | custom
